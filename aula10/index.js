//Dados primitivos

//String, number, undefined, null, booleran, symbol

const nome = 'Luiz'; //string
const nome1 = "Luiz"; //string
const nome2 = `Luiz`;  //string (template string)

const num1 = 10; //number
const num2 = 52.2; //number (float)
let nomeAluno; // undefined = não aponta pra local nenhuma na memória
const sobrenomeAluno = null; //Nulo -> não aponta pra local nenhuma na memória.

const aprovado = false; //boolean = true ou false (valor lógico)

console.log(typeof num1, num1); // informa o tipo e o valor


